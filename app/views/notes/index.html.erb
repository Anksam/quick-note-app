<% @notes.each do |note| %>
  <h3><%= link_to note.title, note %></h3>
  <h5><p>
    <%= time_ago_in_words(note.created_at) %> ago <br/><br/>
    <%= truncate(note.content, length:185) %>
  </p></h5><hr>
<% end %>
