<%= simple_form_for @doc do |f| %>
  <%= f.input :title, class: "form-control "%>
  <%= f.input :content, class: "form-control", placeholder: ""%>
  <%= f.button :submit, class: 'btn btn-primary' %>
<% end %>
